for a in [10, 20, 30] {
    for b in [30, 20, 10] {
        if a < b {
            System.PrintLine "${a} < ${b}"
        } else if a > b {
            System.PrintLine "${a} > ${b}"
        } else {
            System.PrintLine "${a} = ${b}"
        }
    }
}